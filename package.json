{
  "name": "pomodoro1",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "publish": "npm run build && surge ./dist --domain etsy2.surge.sh"
  },
  "devDependencies": {
    "vite": "^7.1.2"
  }
}
